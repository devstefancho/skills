# Implementation Report

| Field | Value |
|-------|-------|
| Task | {task title} |
| Source | `{argument path | "Conversation"}` |
| Task Doc Status | {completed | partial | failed | N/A (no file)} |

## 완료한 기능 (Feature Completed)

{사용자 관점에서 무엇이 가능해졌는지 2-4줄로 요약. 내부 구현이 아닌 외부 동작 중심.}

- {기능 1}
- {기능 2}

## 기술 구현 (Technical Implementation)

- {패턴, 자료구조, 의존성 선택, 아키텍처 등 자명하지 않은 결정만 기재. 없으면 "특이사항 없음".}

## 테스트 (Tests)

- Framework: {detected framework}
- Command: `{test command}`
- Result: **{pass count} passed, {fail count} failed**

### 커버리지
| Requirement / Scenario | Test | Status |
|---|---|---|
| {검증 항목} | {test name} | Pass/Fail |

## 사용자 조치 필요 (Manual Actions Required)

{직접 수행해야 하는 항목. 해당 없으면 "없음".}

- {예: 환경 변수 `FOO` 설정, `npm install` 실행, DB 마이그레이션 적용 등}

## 다음 단계 (Next Steps)

- {우선순위가 가장 높은 후속 작업 1-3개}
